// Deprecated duplicate tests; consolidated into refs.absolute-helpers.test.ts
// Keeping this file temporarily to avoid churn; it is inert and does not execute any active tests.
import { test } from '@jest/globals';

test.skip('deprecated duplicate (migrated to refs.absolute-helpers.test.ts)', () => {
	// no-op
});
